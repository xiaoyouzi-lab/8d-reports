# Current Task

## Task Name

Marketing Analysis v2 and entry-page SEO fix.

## Evidence

This task is based on A-grade first-party exports generated by Marketing Data Pipeline v1:

- GSC shows early visibility for `8d`, `8d report`, `8d reports`, `8d software`, and `8d template`, but average positions are mostly above 30.
- The homepage has 244 GSC impressions and 65 GA4 sessions with an 18.5% engagement rate.
- `/8d-report-template` has 67 GSC impressions at an average position of 51.6.
- GA4 contains `signup_success`, `report_created`, `export_clicked`, and `export_succeeded`, while the funnel expects `sign_up`, `create_report`, and format-specific export events.
- The SERP competitor file is still an empty template, so competitor conclusions remain unavailable.

## Goal

Use the first real GSC / GA4 evidence to improve operating judgment, align the GA4 funnel taxonomy, and strengthen only the two entry pages with demonstrated visibility: `/` and `/8d-report-template`.

## Scope

- Rank-aware weekly report recommendations for positions 1-10, 11-30, and above 30.
- A Measurement Integrity section comparing actual GA4 event names with the expected funnel taxonomy.
- Lightweight GA4 event-name normalization while preserving existing internal product-event names.
- Homepage metadata, search-intent positioning, first-screen value proposition, and CTA improvements.
- A usable D0-D8 template page with filling guidance, common mistakes, format guidance, sample-report links, FAQ, and FAQ JSON-LD.
- Marketing workflow, data dictionary, and development-log updates.

## Non-Goals

- Do not push or open a PR in this task.
- Do not commit live GSC / GA4 CSV exports or Google credentials.
- Do not change auth, payment, subscriptions, pricing, database schema, export core logic, attachment ZIP, AI gating, Vercel environment variables, or production deployment settings.
- Do not claim competitor patterns until the B-grade manual SERP sample has real rows.
- Do not force browser-side `checkout_completed` tracking into the payment flow; document the server-side measurement gap for later work.

## Acceptance Criteria

- Low-ranking zero-click queries are described as early visibility, not a title/meta-only CTR problem.
- Homepage low engagement produces a first-screen value proposition and CTA recommendation.
- The report lists actual GA4 events, expected funnel events, matches, aliases, missing events, and measurement risk.
- Existing internal events remain intact while GA4 receives `sign_up`, `create_report`, and format-specific export click names.
- `/` and `/8d-report-template` accurately describe implemented product behavior and avoid QMS, ERP, MES, PLM, or SSO claims.
- `/8d-report-template` includes FAQ JSON-LD using the project and Next.js schema pattern.
- Full project checks and `npm run marketing:report` pass.

## Risks

- Current GA4 exports cannot separate PDF, Word, and Excel from historical generic export event names without an event-parameter report.
- `checkout_completed` is written by the payment webhook to product analytics, but it is not currently sent to GA4.
- Search rankings are still early; page changes need a new GSC observation window before impact is judged.
- Competitor and GEO positioning still require real B-grade SERP samples.
